DeaDBeeF player Gnome (DBus) multimedia keys plugin
Author Ruslan Khusnullin <ruslan.khusnullin@gmail.com>

It adds support for Gnome multimedia keys to DeaDBeeF player.

To build:
* In Ubuntu/Debian you will need gcc, libdbus-glib-1-dev and deadbeef-plugins-dev packages installed
$ sudo apt-add-repository ppa:alexey-smirnov/deadbeef
$ sudo apt-get update
$ sudo apt-get install gcc libdbus-glib-1-dev deadbeef-plugins-dev

$  sh build.sh

To install:

$ sudo sh install.sh
(copies .so file to /usr/lib/deadbeef/)

