DeaDBeeF player Gnome multimedia keys plugin
Author Ruslan Khusnullin <ruslan.khusnullin@gmail.com>

It adds support for Gnome multimedia keys to DeaDBeeF player.

To build:
* You need deadbeef-plugins-dev package installed to have deadbeef.h

$ ./build.sh

To install:

$ sudo ./install.sh
(copies .so file to /usr/lib/deadbeef/)
